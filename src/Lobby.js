import React, { Component } from 'react'

class Lobby extends Component {

  render() {
    return (
      <div>
        Here's the lobby
        here you will have buttons to start/join new private game
        if you do go in, you will create the game on the back end, and redirect to that game
      </div>
    )
  }
}

export default Lobby
